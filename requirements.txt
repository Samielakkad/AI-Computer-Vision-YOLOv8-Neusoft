# Cœur — réseau, calcul tensoriel, vision
torch>=1.7.1
torchvision>=0.8.2
numpy>=1.21
opencv-python>=4.5
Pillow>=8.0
scipy>=1.7

# Entraînement / logs / progression
tqdm>=4.60
tensorboard>=2.6
matplotlib>=3.4

# Profilage du modèle (summary.py)
thop>=0.1.0

# Export ONNX (predict.py mode export_onnx)
onnx>=1.10
onnxsim>=0.4

# Évaluation mAP COCO (get_map.py map_mode=4) — optionnel
pycocotools>=2.0
